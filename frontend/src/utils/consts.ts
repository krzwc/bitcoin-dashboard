export enum POLLING_INTERVALS {
    CHART = 30000,
}

export const MS_TO_S_FACTOR = 1000;

export const TOTAL_X_TICKS = 10;

export const TOTAL_REFLINES = 8;

export const DOMAIN_FACTOR = {
    MIN: 0.95,
    MAX: 1.05,
};
